import { TextToLowerCaseDirective } from './text-to-lower-case.directive';

describe('TextToLowerCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new TextToLowerCaseDirective();
    expect(directive).toBeTruthy();
  });
});
